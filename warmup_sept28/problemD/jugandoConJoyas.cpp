#include <iostream>
#include <cmath>

using namespace std;

int main(){
    freopen("1.in", "r", stdin);
    freopen("1.out", "w", stdout);
    int N; cin >> N;
    int winner = 1;
    

    cout << (winner ? "Alice" : "Bob");
    return 0;
}